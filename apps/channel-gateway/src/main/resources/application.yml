spring:
  application:
    name: lynxus-channel-gateway
  docker:
    compose:
      enabled: false
  jackson:
    default-property-inclusion: non_null
  datasource:
    url: ${LYNXUS_CHANNEL_GATEWAY_DATASOURCE_URL:jdbc:postgresql://127.0.0.1:5432/lynxus_channel_gateway}
    username: ${SPRING_DATASOURCE_USERNAME:lynxus}
    password: ${SPRING_DATASOURCE_PASSWORD:lynxus}
  flyway:
    enabled: true
    table: channel_gateway_schema_history
    baseline-on-migrate: true
    baseline-version: 0

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  structured:
    format:
      console: logstash
    json:
      context:
        include: true
        prefix: ""
      add:
        service: ${spring.application.name}

server:
  port: ${LYNXUS_CHANNEL_GATEWAY_PORT:8082}

lynxus:
  api:
    base-url: ${LYNXUS_API_BASE_URL:http://127.0.0.1:8080/api}
  channel-gateway:
    base-url: ${LYNXUS_CHANNEL_GATEWAY_BASE_URL:http://127.0.0.1:8082}
    async:
      inbound-session-dispatch:
        core-size: ${LYNXUS_CHANNEL_GATEWAY_INBOUND_DISPATCH_CORE_SIZE:2}
        max-size: ${LYNXUS_CHANNEL_GATEWAY_INBOUND_DISPATCH_MAX_SIZE:8}
        queue-capacity: ${LYNXUS_CHANNEL_GATEWAY_INBOUND_DISPATCH_QUEUE_CAPACITY:1000}
        keep-alive-seconds: ${LYNXUS_CHANNEL_GATEWAY_INBOUND_DISPATCH_KEEP_ALIVE_SECONDS:60}
        shutdown-await-seconds: ${LYNXUS_CHANNEL_GATEWAY_INBOUND_DISPATCH_SHUTDOWN_AWAIT_SECONDS:30}
        wait-for-tasks-to-complete-on-shutdown: ${LYNXUS_CHANNEL_GATEWAY_INBOUND_DISPATCH_WAIT_FOR_TASKS_TO_COMPLETE_ON_SHUTDOWN:true}
    outbound-relay:
      enabled: ${LYNXUS_CHANNEL_GATEWAY_OUTBOUND_RELAY_ENABLED:true}
      scan-fixed-delay: ${LYNXUS_CHANNEL_GATEWAY_OUTBOUND_RELAY_SCAN_FIXED_DELAY:PT5S}
      owner-lock-ttl: ${LYNXUS_CHANNEL_GATEWAY_OUTBOUND_RELAY_OWNER_LOCK_TTL:PT30S}
      connect-timeout: ${LYNXUS_CHANNEL_GATEWAY_OUTBOUND_RELAY_CONNECT_TIMEOUT:PT10S}
      remote-max-pending-finals: ${LYNXUS_CHANNEL_GATEWAY_OUTBOUND_REMOTE_MAX_PENDING_FINALS:100}
      remote-resume-pending-finals: ${LYNXUS_CHANNEL_GATEWAY_OUTBOUND_REMOTE_RESUME_PENDING_FINALS:20}
      native-max-pending-finals: ${LYNXUS_CHANNEL_GATEWAY_OUTBOUND_NATIVE_MAX_PENDING_FINALS:1}
      native-resume-pending-finals: ${LYNXUS_CHANNEL_GATEWAY_OUTBOUND_NATIVE_RESUME_PENDING_FINALS:0}
      transient-queue-capacity: ${LYNXUS_CHANNEL_GATEWAY_OUTBOUND_TRANSIENT_QUEUE_CAPACITY:100}
  agent-runtime:
    base-url: ${LYNXUS_AGENT_RUNTIME_BASE_URL:http://127.0.0.1:8090}
  extensions:
    registration-file: ${LYNXUS_EXTENSION_REGISTRATION_FILE:}
  internal-auth:
    token: ${LYNXUS_INTERNAL_AUTH_TOKEN}
